# 未読み込み・失敗表示の改善計画と進捗

更新日: 2026-09-20

## 目的

護衛が実際には読み込まれているのに「未読み込み」や操作失敗に見える状況を減らす。状態が本当に不明なときは、原因を調べる手掛かりと次の操作を表示する。

## 調査で分かったこと

1. `status()` と `getLoadedMob()` はBukkitのUUID検索だけでEntityを探していた。管理用の再確認処理には、UUID検索が一時的に失敗した場合のチャンク内検索が既にある。表示・操作側は同じ状況を「確認中」または操作不能と扱う可能性があった。
2. `updateManagedChunks()` は所在不明の契約を管理対象から外していた。所有者がオンラインでも保存位置を読み直せず、自然なチャンク読み込みを待つ状態になり得た。
3. GUIの一括操作は「操作しなかった数」を「失敗」と表示していた。モードが既に同じ場合など、正常な対象外も混ざる。
4. チャンク維持には全体・所有者ごとの上限があり、護衛が分散して上限を超えれば未読み込みは発生し得る。ワールド不在、Entityの移動・削除、別プラグインの介入も残る。

## 実施内容

- [x] 表示・操作のEntity検索で、UUID検索が空の場合に保存位置の**既に読み込まれた**チャンクだけを確認する。チャンクの強制読み込みやデータ変更はしない。
- [x] 所在不明の有効な契約を、オンライン所有者のチャンク維持候補から除外しない。既存の上限とサイクルごとの読み込み数は守る。
- [x] 一括操作の文言を実際に数えている「未変更」「未移動」「操作不可」に合わせる。回復済みの数を失敗と呼ばない。
- [x] 未読み込み表示にチャンク維持設定・上限を確認する案内を追加する。
- [x] 利用者による確認項目を以下に記録する。
- [ ] 利用者がWindows 10 / XAMPPとは別のMinecraftテストサーバーで手動確認する。依頼に従い、開発側ではテスト・ビルドを実行しない。

## 今後の改良候補

1. 護衛ごとの「チャンク維持対象外の理由」を表示する。設定無効、所有者オフライン、所有者上限、全体上限、ワールド不在を分けるには、管理サイクルの選定結果を保存する必要がある。
2. 一括操作で未変更の理由を集計する。同じモード、移動先が危険、保存失敗などを別々に数えるには、真偽値だけの操作戻り値を理由付き結果へ変更する必要がある。
3. 所在不明の護衛を探す管理者向け診断を追加する。最終位置・最後の確認時刻・Entity読み込みイベントの観測回数を1画面にまとめる。

## 引き継ぎ時の注意

`guards.yml` の契約をUUID検索失敗だけで削除しない。Entityと保存情報の不一致は隔離処理へ進むため、無条件に利用可能へ変更しない。古い `messages.yml` を運用側で変更済みの場合、新しい一括操作文言は自動上書きされないので、必要なら該当キーを手動で更新する。
