# BodyGuard 安全性改善の実装記録

更新日: 2026-09-20

この文書は `SOL_IMPLEMENTATION_PLAN.md` に沿った実装内容と、実行していない確認を分けて記録します。ユーザーの指示により、自動テスト、Mavenビルド、Minecraft/Spigotサーバー起動、Jar配置、故障注入は実施していません。

## 実装した安全化

### フェーズA: 保存・復旧

- `SafeYamlFile` に `SUCCESS`、`READ_ONLY`、`VALIDATION_FAILED`、`IO_FAILED`、`UNKNOWN_RESULT` の保存結果を追加しました。
- liveファイル、一時ファイル、バックアップのルート、version、record-countを検証します。
- 破損したliveファイルは `.damaged-...` として保全してから `.bak` を復旧します。復旧不能時は空データで上書きしません。
- `GuardStorage` は護衛ごとの不正レコードを `quarantine.yml` に保全できた場合だけ読み飛ばします。`PlayerDataStorage` はチュートリアル、相棒、仲間を項目単位で扱います。
- `operations.yml` に解除・完全削除・死亡の操作ID、契約世代、受理時刻、完了時刻、checksumを保存します。

### フェーズB: ライフサイクル・整合性

- `GuardData` に契約状態、観測状態、契約世代、最終確認時刻、所在不明観測数を追加しました。
- UUID検索失敗、未読み込み、ワールド不在、確認中、所在不明、隔離を分け、通常の問い合わせでは登録・削除・保存・チャンクロードを行いません。
- Entity読込・起動時走査だけがPDCとの整合・再構成を行います。レジストリにないPDC付きMobはPDC単独で勧誘・再登録せず、解除済み・削除済み・死亡済みの履歴と操作台帳を優先します。
- PDC、所有者、種類、UUID、操作台帳の矛盾は推測で移管・削除せず隔離します。再起動後も隔離記録をPDCで上書きしません。
- 処理待ち・完了済みEntityを再確認するときもPDCのguard UUID、所有者、種類、契約世代を照合し、識別マーカーや元設定スナップショットが欠けていれば変更せず隔離します。
- 独立台帳だけが残る状態から明示的に再勧誘する場合は、台帳より新しい契約世代を採番して旧操作を新契約へ再適用しません。
- 解除・完全削除は、実行直前の所有者・状態確認、台帳保存、レジストリ保存、Entity処理、完了保存の順序を守ります。保存失敗時は保留・再試行にします。

### フェーズC: GUI・コマンド

- `/bg status` と `/bg status server`、タブ補完、管理者向けの保存・台帳・チャンク診断を追加しました。
- GUIの履歴カードは表示専用です。確認画面は対象UUIDのスナップショットを保持し、セッションごとに確定を1回だけ受け付けます。
- 実行直前に所有者、権限、UUID、状態を再確認します。Shiftクリック、数字キー、ダブルクリック、ドラッグ、オフハンド交換などの持ち出しをキャンセルします。
- 保存できない解除は通常の解除成功と表示せず、解除予約・失敗として表示します。

### フェーズD: 移動・登録失敗

- `LocationUtil` はEntityの幅・高さ、足場、通過可能体積、危険ブロック、ワールド境界、他のLivingEntityとの重なり、読み込み済みチャンクを確認します。
- 安全地点が見つからない場合は移動しません。召喚・勧誘の登録失敗時はPDCと元Mob設定を戻し、孤立Entityを残しません。

### フェーズE: 例外・負荷・reload

- 定期処理、起動時走査、Chunk読込、死亡イベント、一括操作は護衛単位で例外を分離し、同じ失敗のログを抑制して再試行時刻を管理します。
- チャンクチケット、探索チャンク、1周期のロード数、所有者ごとのチケット数に上限を設け、終了時にチケットを解放します。
- `config.yml` と `messages.yml` は一時検証後に反映し、不正なreloadで現在の設定やGUIタスクを壊さないようにしました。

## 静的レビューで確認した入口

- GUI描画、一覧フィルター、チャット一覧、戦闘判定、アクションバーは読み取り用APIを通ります。
- 起動時、ChunkLoadEvent、EntitiesLoadEvent、ChunkUnloadEvent、死亡イベント、解除・削除コマンド、GUI確認、定期処理、停止処理を確認しました。
- `config.yml`、`messages.yml`、`plugin.yml`、タブ補完、README、`MANUAL_VERIFICATION.md` を実装内容に合わせました。
- `git diff --check` は実行済みです。コンパイルを伴う確認はしていません。
- ソース上で、読み取りAPIがEntity登録・削除・保存・チャンク強制読み込みを行わないこと、処理待ちEntityの再照合、台帳の契約世代、停止時の各後処理の独立実行を確認しました。

## 未実施・残る確認

- Javaコンパイル、Mavenビルド、自動テスト、Spigot実機テスト、保存失敗の故障注入は未実施です。
- Spigot 1.21.8上でのイベント発火順、チャンクチケットの実際の保持、Entity体格判定、各GUIクリックの挙動は未確認です。
- 実機確認は、本番ではなく検証用サーバーで [MANUAL_VERIFICATION.md](MANUAL_VERIFICATION.md) のT01～T30を使って行ってください。

## 完了報告で誤解しないためのチェック

- [x] 保存結果を呼び出し元へ返し、保存失敗を成功表示にしない経路を追加した。
- [x] 不明なEntity、未読み込みワールド、矛盾PDCを推測で削除しない経路を追加した。
- [x] 解除・削除・死亡の受理記録と再登録防止を追加した。
- [x] 診断コマンド、チャンク上限、再試行、手動確認書を追加した。
- [ ] Javaコンパイルが通ることを確認した。
- [ ] Minecraft実機でT01～T30を確認した。

「実装した」はソースの静的確認を意味し、「動作確認済み」や「すべてのエラーが起きない」という意味ではありません。
