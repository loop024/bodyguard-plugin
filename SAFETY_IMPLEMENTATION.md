# 安全性改善の実装記録

## 引き継ぎ（2026-09-16）

ユーザーの指示により、実装途中でSol向け開発計画の作成へ切り替えた。
詳細な仕様、現状の既知問題、実装順序、完成条件、手動確認手順は
[SOL_IMPLEMENTATION_PLAN.md](SOL_IMPLEMENTATION_PLAN.md) を参照すること。
現時点のコードは未完成であり、コンパイル・自動テスト・実機テストは未実施。
以下の項目は一部コードが追加済みでも、全経路の実装と静的確認が終わるまでチェックしない。

ユーザー指定により、自動テスト・実機テストは実行しない。完了確認はコードの静的確認とし、実機確認手順を README に記載する。

## Sol実装開始（2026-09-16）

フェーズAの最初の安全化を開始した。まだフェーズ完了ではないため、下の完成項目にはチェックを付けない。

- `SafeYamlFile`: 保存結果を成功・読取専用・検証失敗・I/O失敗・結果不確定に分類し、最終試行時刻、連続失敗数、失敗理由を保持するようにした。一時ファイルは本番置換前に再読込して検証する。
- `GuardStorage`: 未対応の新しいスキーマを上書きせず停止する。壊れた護衛レコードは `quarantine.yml` への保全が成功した場合だけ読み飛ばす。空の正常データにもルートと件数を書く。
- `PlayerDataStorage`: チュートリアル、相棒、仲間を項目別に読み込み、一項目の不正値で同じプレイヤーの残り設定を失わないようにした。
- `GuardManager`: Entityを受け取る照会APIから、登録削除、解除・削除完了、PDCからの暗黙再登録を除いた。これらの整合処理は起動時走査とEntity読込イベントに限定した。新しい解除・削除受付時は完了フラグをリセットし、保存失敗時は元の値へ戻す。

未実施: 操作台帳、完全な状態列挙への移行、隔離データの重複抑制、保存結果の全GUI/コマンド接続、フェーズB以降。コンパイル・テスト・実機確認も未実施。

## 実装対象

- [ ] 護衛の状態分類、最終確認情報、所在不明の分離
- [ ] 削除・解除の永続記録と再登録防止、保存前の破壊的操作防止
- [ ] 死亡・解除カードと件数の整合、一覧整理と管理欄
- [ ] YAML破損時の保全・バックアップ復旧・変更停止
- [ ] 未読み込みワールドの識別子と座標の保持
- [ ] 保存失敗の通知・再試行、相棒・仲間設定の保護
- [ ] 安全な移動先がない場合の中止、体格・危険床・境界の判定
- [ ] 個体単位の例外処理、召喚・勧誘のロールバック
- [ ] 二重操作防止と実行直前の検証
- [ ] 不正な設定再読み込み時の正常設定維持
- [ ] チャンク読み込み・探索の上限と分散
- [ ] 管理者向け診断とログ抑制
- [ ] 初心者向け手動確認手順、全変更の静的レビュー
