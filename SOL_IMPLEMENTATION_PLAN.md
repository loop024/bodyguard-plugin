# BodyGuard 安全性・使いやすさ改善 — Sol向け実装引き継ぎ計画

作成日: 2026-09-16 / 対象: `C:\Users\USER\Documents\Plugin\BodyGuard`

## 0. この文書の位置づけと最初に守ること

ユーザーは、護衛一覧に存在しない護衛が残る問題を含め、提案した改善をすべて実装するよう依頼した。その途中で「ここからはSolに実装させるので、詳細な開発計画書を書いて」と指示した。この文書は、その実装を最後まで引き継ぐための仕様・作業順・未完事項・完成条件である。

**現状は実装途中。完成品として扱わない。コンパイル、自動テスト、実機テストはまだ実行していない。** 以下の「追加済み」はソースにコードがあるという意味であり、動作確認済みという意味ではない。

### 必須の制約

- ユーザーの明示指示: **テストはユーザー自身が行う。エージェントはテストを実行しない。** 自動テスト・ゲーム内実機テスト・故障注入を勝手に実行しない。
- ビルドによるコンパイル確認はテスト実行とは分ける。行う場合はテストのコンパイル・実行を省略するオプションを明示する。現時点ではそれも未実施。
- 実装後、初心者が操作できる手動確認手順を日本語で書く。検証していないことを検証済みと報告しない。
- Windows 10、Minecraft Java Edition / Spigot 1.21.8、Java 21、Maven。既存の `pom.xml` に合わせる。Paper専用API、NMS、外部の必須プラグインを増やさない。
- ユーザーは一般のWeb開発環境としてXAMPPを使用するが、このリポジトリはJavaのMinecraftプラグイン。PHP/Pythonのアプリに作り替えない。
- 既存の戦闘AI、攻撃力、移動速度、権限、メニューアイテム保護、お気に入り、相棒、仲間、名前入力、ガイドを維持する。
- リポジトリの `MinecraftPluginTemplate` は今回の実装対象外。
- 不明な護衛を勝手に完全削除しない。正常な護衛を失わないことを最優先する。
- 勝手にサーバー起動、Jarの本番配置、設定データの破損、バックアップ削除をしない。
- 今回は追加エージェントを起動する指示は受けていない。Solが通常の実装として継続する。

### Gitに関する注意

作業途中に `auto-backup: 2026-09-16 19:02:00 JST`、`auto-backup: 2026-09-16 19:07:00 JST` というコミットが作成されていることを確認した。したがって、**`git diff` に出ない変更も、この改善作業で追加されたコードである。** `git status` が変更3ファイルしか示さなくても、全体が完成しているという意味ではない。

開始時に `git status`、直近ログ、下記の各ファイルの実体を読む。過去コミットに一括で戻したり、未コミット変更を破棄したりしない。行番号は変更で動くため、以下では主にクラス名・メソッド名を使用する。

## 1. 目標と守る不変条件

### ユーザーに提供する最終結果

1. 死亡・解除・削除済みの護衛は、通常一覧と通常の護衛数から外れる。
2. 遠方の正常な護衛、未読み込みワールド内の護衛を誤って消さない。
3. 所在不明の護衛は独立した表示に移り、最終確認時刻・場所・次の操作が分かる。
4. 削除・解除した護衛が、チャンク読み込みや再起動によって復活しない。
5. 保存が失敗・破損しても、空データで元データを上書きしない。復旧不能時は安全に変更を停止する。
6. 安全な移動先が見つからないときは召喚・呼び戻しを中止する。
7. 1体の不具合で他の護衛の操作・定期更新を巻き添えにしない。
8. 連打、古いGUI、権限変更、処理途中の死亡などでも二重処理・誤対象操作がない。
9. チャンク読み込み・探索量に上限があり、後ろの護衛も順番に処理される。
10. 管理者は保存状態、所在不明、処理待ち、負荷制限の状況を確認できる。

### 実装で絶対に崩さない条件

- UUIDの検索失敗、`isValid() == false`、チャンクが読み込み済みという事実だけで死亡を断定しない。
- チャンク本体の読み込みとEntityの読み込みは別段階として扱う。
- 表示・診断・フィルターのための問い合わせは、Entity削除、登録解除、保存、チャンク強制読み込みなどの副作用を起こさない。
- 削除・解除の意思を永続化してから実体を変更する。永続化に失敗した操作を「成功」と表示しない。
- 完了済みの削除・解除記録は、未読み込みEntityや旧PDCからの再登録を防ぐために残す。短いTTLや件数制限で安全記録を消さない。
- 別の所有者に属するEntityやUUIDの不整合を見つけたら、推測で移管・削除せず隔離して報告する。
- チャンク、Entity、Player、Inventoryを触る処理はサーバーのメインスレッドで行う。
- 一覧の自動更新で別の護衛を同じスロットへ詰めない。対象UUIDを固定する。
- 読み込み不能ワールドのUUIDと座標を保持する。同名の別ワールドに自動で付け替えない。
- 例外の握りつぶしや「すべてcatchすれば安全」という実装をしない。保存状態、処理結果、再試行可否を明確にする。

## 2. 現在のコードと未完状況

Javaの基準ディレクトリは `src/main/java/plugin/test/com/bodyGuard/`。

| ファイル | 今ある変更・役割 | 完成までに必要なこと |
|---|---|---|
| `storage/SafeYamlFile.java` | YAML読み込み、破損コピー、`.bak`復旧、一時ファイル置換、保存エラー通知・30秒抑制 | スキーマ検証、正常バックアップ保護、復旧中断時の保護、世代管理、状態・結果型を完成させる |
| `storage/GuardStorage.java` | SafeYamlFile使用、状態・最終確認・SavedPosition保存 | 不正項目の隔離、バージョン移行、操作記録の安全な永続化 |
| `storage/PlayerDataStorage.java` | SafeYamlFile使用、dirtyと再保存 | 保存結果の呼び出し元への返却、項目別の隔離、独立した再試行 |
| `guard/SavedPosition.java` | World参照なしでUUID・名前・座標を保存 | 全経路での検証、PDC連携、旧データ互換 |
| `guard/GuardData.java` | Status列挙、最終確認・所在不明時刻・死亡・操作完了フラグ | フラグ矛盾をなくす状態遷移、保存・表示状態の整理 |
| `guard/GuardManager.java` | 状態照会、履歴、削除前保存、解除前保存、死亡記録、登録ロールバックの土台、探索・チャンク上限の土台 | 下記の重大な抜け道、結果型、所有者確認、副作用分離、公平な処理を修正 |
| `guard/GuardTask.java` | 個体単位try/catch、定期処理の一部を保護、体格対応移動呼び出し | 保存独立リトライ、ログ抑制、処理量・失敗個体の再試行制御 |
| `gui/BodyGuardMenuHolder.java` | MISSING / HISTORYフィルターを追加 | GUIセッション識別・二重確定防止 |
| `gui/GuardListQuery.java` | 通常一覧からMISSINGを分離、HISTORY判定 | 集計の一貫性、読み取り副作用の除去 |
| `gui/BodyGuardGui.java` | 所在不明・履歴入口、最終位置・時刻、履歴カード、「一覧を整理」 | 履歴集計、表示文言、操作制御、二重クリック、結果詳細を完成 |
| `util/LocationUtil.java` | 体格範囲、危険ブロック、境界、未ロードチャンク、候補なしnull | 実際の衝突形状、足場、既存Entityとの重なり、安全失敗理由 |
| `command/BodyGuardCommand.java` | 召喚先なしの中止、危険なフォールバック削除 | 診断コマンド、一括結果、保存エラー・移動理由表示、例外の局所化 |
| `BodyGuard.java` | 起動時保存読込失敗の停止、終了時プレイヤー再保存、負荷制限getter | 設定の検証付き再読込、停止経路の保護、独立リトライタスク |
| `SAFETY_IMPLEMENTATION.md` | 作業チェックリスト | 完成時に根拠とともに更新する。現状は全項目未チェック |

### 最初に直す具体的な問題

以下は現在のソースを読んで確認した未完事項・構造上の問題。実機で再現確認したという意味ではない。

1. **保存状態を操作入口で統一して確認していない。** `isStorageHealthy()` はあるが、コマンド・GUI・Managerの変更操作全体に接続されていない。保存失敗後もモード変更等が成功表示になる経路がある。
2. **読み取りが変更を起こす。** `getLoadedMob()` → `getGuardData(Entity)` が、PDCなしで登録を消したり、保留中の解除・削除を実行したりする。表示や集計から呼ばれるため、読み取りAPIと整合処理を分ける必要がある。
3. **履歴の `operationCompleted` を新しい操作時にリセットしていない。** 解除完了履歴を管理者の全削除対象にすると、実体未発見でも削除完了のように表示し得る。操作種別変更時の状態遷移を明示する。
4. **バックアップは構文とルートの簡易判定だけ。** 意味的に壊れたファイルや、既存データが空に切り詰められたファイルを正常扱いし、良いバックアップを置き換える可能性がある。
5. **不正な護衛項目はログを書いてskipするだけ。** 次の保存で元項目が消える。rawデータと理由を隔離先へ保存してから有効データから外す。
6. **プレイヤーデータは項目ごとの例外分離が不十分。** 相棒UUIDなどの不正値が同じプレイヤーの仲間読み込みを巻き込む。個々の項目を分けて検証する。
7. **SavedPositionを追加したがPDCやGUIの警備地点処理は旧設計が残る。** `applyPdc()` は解決できないanchorを未設定扱いし得る。`guardPointPlaceholders()` は解決不能時にプレイヤー位置を表示しており、登録場所の誤表示になる。
8. **所在不明の計測がEntity読み込み完了と連動していない。** `missingSince` は未ロード中にも設定され得る。ロード直後に古い時刻で所在不明判定をしないよう直す。
9. **探索の公平性がない。** `searchBudget` はあるが、毎回先頭から走査すると先頭の不明個体が予算を使い切る。護衛単位の巡回カーソルが必要。
10. **完了履歴もcleanupの探索対象。** 永続履歴が増えるほど探索を消費する。完了済み履歴はEntity読み込みイベントからだけ照合する。
11. **`searchOffsets` の寿命管理がない。** 生きた対象だけに限定し、解除等で掃除する。
12. **チャンクチケットの付け替え順序。** 現在は新しいチケットの追加が古いチケット解放より先。移動時に一時的に上限を超え得る。不要分解放→残り容量計算→取得にする。
13. **負荷制限getterに対応するconfig項目・説明が未追加。** 所有者ごとの割り当て公平性も必要。
14. **履歴画面とヘッダーの母集団が異なる。** HISTORYページのデータは履歴だがヘッダーは現役護衛。空の履歴で「最初の護衛を召喚」が出る可能性もある。専用の空表示・件数・説明にする。
15. **一括回復・呼び戻し・モード変更すべての個体単位例外分離は未完。** 定期タスクの一部だけで済ませない。
16. **登録ロールバックは土台のみ。** 保存APIの失敗が「確実に未保存」か「保存済みか不明」かを区別し、PDC・レジストリ・生成Entityの戻し方を詰める。
17. **安全移動は保守的なブロック判定のみ。** `isSolid()` は足場形状の完全な保証ではない。召喚位置の複数Mob重なりも解決していない。
18. **設定再読み込み、診断、GUI連打対策はまだ追加していない。** 保存再試行が通常の自動保存無効時にも動く保証もない。
19. **ドキュメントは旧仕様の記載が残る。** `messages.yml` の古いキーが新しいフォールバック文言を隠す箇所もある。
20. **不要import・旧ヘルパーが残る。** 例: GuardStorageの旧Location入出力関連import。整理は安全性実装後に実施する。

## 3. 完成させるデータモデル

### 3.1 永続状態と観測状態を分離する

最終的には「契約の状態」と「現在実体が見えるか」を分ける。現在追加した複数booleanをそのまま増やし続けない。

推奨する永続状態:

| 状態 | 通常一覧 | 上限への算入 | 再発見時 |
|---|---|---|---|
| ACTIVE | 通常表示。ただし所在不明は専用表示 | 算入する | UUID・所有者・種類等を照合して利用可能に戻す |
| RELEASE_PENDING | 履歴・処理待ち | 算入しない | 元の設定を復元してPDCを解除 |
| RELEASED | 履歴 | 算入しない | 古い護衛PDCがあるときだけ解除を再適用 |
| DELETE_PENDING | 履歴・処理待ち | 算入しない | 対象の実体を削除 |
| DELETED | 履歴 | 算入しない | 古い実体が再発見されても再登録しない |
| DEAD | 履歴 | 算入しない | 古い保存実体を通常の護衛として復活させない |

観測状態は ACTIVE に対して求める:

- AVAILABLE: 生存Entityと所有権・登録が一致する。
- UNLOADED: 最終位置のチャンク内Entityが未読み込み。存在しないとは扱わない。
- WORLD_UNAVAILABLE: 保存ワールドUUIDに対応するワールドが現在ない。
- CHECKING: 起動直後・ロード途中・移動直後・検索予算待ち。
- MISSING: ロード完了後、猶予を置き、複数回の観測で見つからない。永久削除の証拠には使わない。
- QUARANTINED: 破損・所有権矛盾・種類不整合等で安全に操作できない。管理者が確認する。

### 3.2 所在不明と護衛上限

仕様を曖昧にしないため、**所在不明の契約は枠を確保したままにする。** 通常一覧では別表示にするが、上限から無条件には外さない。概要に「契約中8体（所在不明2体を含む）／上限10体」のように表示する。

枠を解放したい所有者は所在不明カードの詳細から「契約解除」を行う。その意思が保存できた時点で枠を解放し、後から見つかっても再登録しない。これにより、不明扱いを利用した上限回避や再発見時の上限超過を防ぐ。OPの召喚無制限・勧誘の既存制限は維持する。

### 3.3 保存項目

- 護衛UUID、所有者UUID、種類、名前、連番、モード、お気に入り。
- anchor / lastのワールドUUID・表示名・XYZ・yaw/pitch。ワールド未解決でもraw情報を保持。
- `last-seen` は確かに存在を確認した時刻。検索失敗で更新しない。
- `missing-since` は読み込み完了後の未発見観測開始。未ロード時間を所在不明の猶予時間に含めない。
- 契約状態、操作ID、操作種別、受理時刻、完了時刻、最後の失敗理由。
- スキーマバージョンと保存リビジョン。
- 生きたEntity参照やBukkit Worldオブジェクトは永続化しない。

旧 `release-pending` / `deletion-pending` / `operation-completed` / `death-confirmed` からの移行規則を1か所に実装する。両pendingがtrueのデータは推測で実行せず隔離する。上位バージョンを勝手に古い形式で上書きしない。

## 4. 実装フェーズA: 保存・復旧を先に完成させる

### A1. 保存結果と状態

`SafeYamlFile` に結果型を導入し、少なくとも成功・I/O失敗・検証失敗・読み取り専用・結果不確定を区別する。正常保存時刻、最後の試行時刻、失敗回数、失敗理由、保留変更の有無を診断に渡す。

`GuardStorage` / `PlayerDataStorage` の変更APIは保存結果を返す。画面は次の区別を守る:

- 永続化できた変更: 成功。
- 安全に保留可能な未保存変更: 「変更は未保存です。再試行中」。完了と断言しない。
- 削除・解除・新規登録の受理を永続化できない: 実行しない。
- 保存内容が不確定: 対象を操作保留にし、ログ・診断に出す。

### A2. 読み込み手順

1. 対象ファイルの存在・内容・スキーマを検証する。
2. 初回の未作成ファイルと、既存データが壊れて空になった状態を区別できる設計にする。正常な空データは明示的なversion/ルート/件数情報を持つ。
3. 破損時は元ファイルを一意名で保全する。保全できなければ元を置き換えない。
4. バックアップも同じスキーマ・項目検証を通す。
5. 復旧候補を一時ファイルへ書き、検証後に置換する。復旧中のクラッシュで唯一の良いコピーを失わない。
6. 復旧不能ならレジストリを空で起動せず、プラグインの変更処理を停止する。起動途中の `onDisable()` が空データを保存しないことを明示的な初期化完了フラグで保証する。

### A3. 不正な項目の隔離

`quarantine.yml` などに、元のパス、rawデータ、理由、日時を保存する。隔離ファイルの保存に失敗したら元データを書き換えない。正常なレコードは引き続き使える設計を基本とするが、安全に分離できないルート破損や操作台帳の破損は変更停止にする。

同一プレイヤーのチュートリアル状態・相棒UUID・仲間UUIDは別々に検証する。一項目の異常で他項目を落とさない。

### A4. バックアップと削除履歴の巻き戻り

通常の`.bak`を復元すると、最新の削除記録が失われ、古いEntityのPDCから護衛が復活する危険がある。ここを通常のバックアップだけで済ませない。

推奨: 削除・解除・死亡の操作記録を独立した追記台帳に保存し、連番・操作ID・UUID・所有者・種別・チェックサムを付ける。受理記録をflush/forceしてから実体変更する。読み込み時は復旧したレジストリに台帳を再適用する。全ファイルの同時原子保存ができると仮定しない。

- 末尾の未完レコードと、中間の破損・欠落を区別する。
- 確定済みの操作を失った可能性がある場合は、PDCから無条件復元せず変更停止・管理者通知にする。
- 同じ操作IDの再適用は何度実行しても結果が変わらないようにする。
- 台帳の圧縮は確定済みの世代を安全に置換できる設計に限る。今回必須なのは履歴の安全であり、複雑な圧縮機能の追加ではない。
- ファイルI/Oが例外を返しただけで「何も書かれていない」と推測しない。受理が不確定なら照合してから再試行する。

### A5. 再試行と停止

保存リトライは `storage.autosave-seconds: 0` でも必要。通常の定期保存とは別に、未保存変更だけを間隔を空けて再試行する。停止時の最終保存、タスク停止、GUI終了は片方が失敗しても残りの後片付けを行う。失敗を無限に毎tick出力しない。

**Aの完成条件:** 正常ファイルを壊れたバックアップで置き換えず、削除意思を復旧で失わず、保存失敗が利用者の成功表示に化けず、起動失敗時に空保存しない。

## 5. 実装フェーズB: 護衛ライフサイクルと整合性

### B1. 問い合わせと変更の分離

`findLoadedMob`、`getStatus`、`getActiveGuards`、`getHistory` は読み取り専用にする。PDC修復、再登録、解除・削除は `reconcileEntity` など明示的なメソッドからだけ実行する。GUIでカードを描画しただけで契約が変わらないようにする。

読み込みイベント、起動時の既存Entity走査、限定された定期整合処理を同じreconcileに接続する。UUIDが一致しても所有者・PDC種別が矛盾する場合は隔離する。通常のレジストリ内容と古いPDCが違う場合は、保存リビジョンと操作台帳で優先順位を決める。

### B2. 削除・解除の順序

```text
権限・所有者・UUID・現在状態・確認対象を再確認
→ 重複操作を除外
→ 操作ID付きの意思を永続化
→ 永続化成功後に通常一覧と上限から外す
→ 読み込み済み実体があれば操作
→ 実体がなければ処理待ち
→ 完了を記録（失敗しても受理済み意思は残る）
→ 後日のEntitiesLoadで同じ操作を安全に再適用
```

遠方の削除対象を探すために全チャンクをロードしない。実体未確認を完了件数に入れない。「指示を保存した」と「Mobの削除まで完了した」は別に表示する。

解除後の通常Mobを所有者が明示的に再勧誘することは可能にする。この場合は新しい契約世代を永続化し、古い解除命令が新しい契約を解除しないようにする。別プレイヤーによる正規の再勧誘の扱いも、旧所有権が解除完了していることを確認してから処理する。現コードの単純な旧owner比較をそのまま最終仕様にしない。

### B3. 死亡・外部削除・変身

- 死亡はEntityDeathEventのUUIDを基準に記録する。PDCが先に変更されても扱えるようにする。
- 外部プラグインによるremove等、死亡通知のない消失はまずMISSING。利用可能なSpigotイベントがあるかローカルAPIで確認し、Paper専用イベントは使用しない。
- Mob変身でUUIDが変わる場合の扱いを明示する。安全に契約を移せない場合は旧対象を終了・新対象を未登録として通知し、無断の二重登録を防ぐ。引き継ぐなら旧新UUID間の対応を1操作として保存する。
- 一時的な別ワールド移動中のinvalidラッパーを死亡扱いしない。
- 相棒の解除は契約終了後の派生データ更新として扱い、片方のファイル保存失敗でも次回起動時に整合できるようにする。

### B4. 所在不明の判定

- EntitiesLoadEvent等でEntity読み込み完了を記録する。ChunkLoadだけで不在を確定しない。
- 起動直後・再ロード直後はCHECKINGに戻す。
- 読み込み完了後の複数回確認＋既定30秒程度の猶予でMISSINGへ移す。検索予算不足は未発見の証拠として数えない。
- 正常Entityを再発見したら自動でAVAILABLEへ戻し、同一UUIDのまま最終確認情報を更新する。
- Worldがない場合はWORLD_UNAVAILABLE。永久削除も猶予時間の消費もしない。
- PDCを失った実体が見つかった場合は、登録記録を即削除せず不整合として扱う。

**Bの完成条件:** 遠方・ワールド未読込・検索遅延で誤削除せず、削除・解除・死亡済み護衛が旧データから復活せず、一覧取得だけでデータを変更しない。

## 6. 実装フェーズC: GUI・コマンド・誤操作防止

### C1. 一覧と履歴

- 通常一覧はACTIVEを対象にし、MISSINGだけ専用フィルターへ分離する。未ロードの正常護衛は理由付きで残す。
- 「通常一覧」「所在不明」「処理待ち・履歴」の対象件数をそれぞれの母集団で計算する。
- 通常一覧のヘッダーは契約数・操作可能数・未読込数・所在不明数・上限・空き枠を表示する。履歴数を上限に加えない。
- 履歴が0件なら「まだ処理待ち・履歴はありません」。召喚の初回案内を出さない。
- 死亡等のカードは表示中の元スロットに終了理由を残し、クリック不能にする。自動で別の護衛を詰めない。
- 「一覧を整理・更新」、ページ移動、再表示で再抽出する。フィルター・並べ替えを維持し、最終ページが空になった場合は有効ページへ補正する。
- 履歴カードは通常詳細の操作ボタンへ進ませない。日時・理由・処理状態を読む専用表示か、操作不能の説明にする。
- 最終位置はSavedPositionのraw情報から表示する。未解決の警備地点をプレイヤーの現在地に置き換えない。
- 不明の原因ごとに次の行動を案内する。「未読み込みなので移動」「ワールド復帰を管理者に依頼」「保存失敗で操作停止」「安全な呼び戻し先なし」を同じ文にまとめない。
- 色だけに頼らず状態名を表示する。新しい日本語文言はmessages.ymlにキーを作り、既存ファイルにキーがなくてもフォールバックする。

### C2. 操作の共通検証

コマンドとGUIは同じManagerの操作APIを使う。入口と実行直前で次を検証する:

1. プレイヤーがオンラインで利用権限を持つ。
2. 対象UUIDと所有者が一致する。
3. ACTIVEまたはその操作が許される状態である。
4. 操作に必要なEntityが存在し生存している。
5. 保存状態が操作を許す。
6. 確認画面・名前入力画面のセッションが現在有効。

一括確認画面は開いた瞬間のUUID集合を固定し、後から増えた護衛を巻き込まない。重複UUIDは除外。確定トークンを一度消費したら同じ画面の再確定を受け付けない。GUI遷移は必要なら次tickへ送るが、その間にもセッションが有効か再確認する。

軽いクリック間隔制限はプレイヤーごとに設け、ログアウト時に掃除する。召喚の連続使用を完全に禁止せず、二重イベントや同tick連打を抑える。Shift、数字キー、ダブルクリック、ドロップ、オフハンド、ドラッグ等でGUIアイテムが持ち出せない既存保護を維持する。

### C3. 操作結果の型

boolean/intだけで原因が消えるAPIを整理する。例えば `OperationResult` に以下を保持する:

- 対象UUID、結果種別、表示用理由キー。
- 成功、受理済み保留、既処理、全回復済み、未読み込み、ワールド不在、所在不明、権限不足、所有権不一致、保存失敗、安全地点なし、他プラグインによる中止、内部エラー。
- 一括結果は同じ対象を複数カテゴリに数えず、合計が対象数と一致する。

呼び戻しの成功は実際にteleportがtrueを返した場合だけ。全回復済みは失敗ではなく対象外。永続化失敗は成功数に加えない。既存の結果アイコン・タイトル・音の仕組みに接続する。

### C4. 診断

`/bg status` を追加し、一般ユーザーには自分の状況、管理者には全体状況を表示する。必要に応じて `/bg status server` を管理者限定にする。タブ補完・help・plugin.yml権限説明を更新する。

表示内容:

- ACTIVE、AVAILABLE、UNLOADED、WORLD_UNAVAILABLE、CHECKING、MISSINGの数。
- 削除待ち・解除待ち・完了履歴・隔離の数。
- 保存正常/停止/再試行中、最終保存時刻、最後の失敗理由。
- チャンクチケット数／上限、処理待ちキュー数、制限で保留中の数。
- 復旧した場合は復旧元・日時、操作台帳の整合性。

診断は読み取り専用。他人の詳細UUIDや保存内容は一般ユーザーに開示しない。汎用の「全部修復・全部消去」コマンドは作らない。再確認ボタンを設けるなら読み込み・探索予算を守る。

**Cの完成条件:** 件数・カード・操作対象が一致し、履歴を操作できず、クリックの再送でも二重実行せず、失敗理由と次の操作が分かる。

## 7. 実装フェーズD: 安全な移動と登録失敗の後始末

### D1. 移動先検索

現在の `LocationUtil.findSafeLocation` のnull返却方針は維持する。安全地点なしでプレイヤー座標や要求座標にフォールバックしない。

- 移動するMobの幅・高さに合わせて衝突範囲を確認する。中心の足・頭の2ブロックだけで済ませない。
- world最小/最大高度、ワールド境界、足場の形状、天井、危険床、液体、炎、ポータル、サボテン、マグマ、焚火、粉雪等を判定する。
- `isSolid()` / `isPassable()` だけの判定で足場を保証しない。Spigot APIの衝突形状・BoundingBox等、対象バージョンで利用できる手段を確認する。
- 安全確認のために未ロードチャンクを無制限にロードしない。
- 同じ一括呼び戻し・連続召喚で使用した座標を予約し、他の実体との極端な重なり・窒息・crammingを避ける。
- 候補探索数は有限。安全地点なしを明確な失敗理由として返す。
- teleportがキャンセル/falseの場合は成功表示せず、last位置・警備地点・戦闘状態を成功扱いで更新しない。
- 別ワールド移動を禁止する既存設定を、単体GUI・一括・コマンド・自動追従の全入口で確認する。

### D2. 召喚・勧誘

新規召喚と既存Mobの勧誘の後始末を分ける:

- 新規召喚: 登録失敗ならその操作で作成したMobだけを撤去する。生成イベントで他プラグインが中止/削除した場合を確認する。
- 勧誘: 登録失敗なら対象を削除せず、元の名前・名前表示・persistent・removeWhenFarAway・aware・対象など変更した設定を戻す。
- 変更前の値をスナップショットし、どの段階まで実行済みか管理する。
- UUIDとPDCの登録、管理Map、保存データが食い違わないようにする。ロールバック自体が失敗した場合は隔離して管理者に報告する。
- 外部プラグインの設定を上書きして元に戻せなくなる実装を避ける。
- 所有者・allowed-mobs・対応種類・上限・保存可否を直前に確認する。

**Dの完成条件:** 安全地点なしなら何も移動せず、召喚失敗で孤立Mobが残らず、勧誘失敗で元Mobを失わない。

## 8. 実装フェーズE: 例外分離・負荷制限・設定再読み込み

### E1. 個体単位の例外処理

定期更新、一括モード、一括回復、一括呼び戻し、一括解除・削除、起動時走査、Entity読込イベントの各ループで、1体の例外が次の個体を止めないようにする。例外は処理結果に含め、操作種別＋対象UUID＋理由をログへ残す。

同じ障害は30秒程度の抑制と再試行間隔を設ける。例外を起こし続ける1体に毎tick処理を使わず、個体別バックオフする。抑制中の件数は診断に出せるようにする。回復後は保留・警告状態を解除する。

### E2. チャンクと探索

config.ymlへ次の既定値と説明を追加する。範囲は既存getterと整合させる:

```yaml
guard-management:
  keep-chunks-loaded-while-owner-online: true
  max-loaded-chunks: 64
  max-loaded-chunks-per-owner: 16
  chunk-loads-per-cycle: 2
  search-chunks-per-cycle: 32
```

- 全体上限と所有者上限を同時に守る。
- 1回の更新でロードする数を制限する。上限超過の対象は保留として説明し、失敗や消失と決めつけない。
- 不要チケットを先に返す。取得APIの結果を確認してから所有チケット集合に追加する。
- 他プラグインのチケットは外さない。
- ログアウト、契約終了、設定無効化、ワールドアンロード、プラグイン停止で不要チケットを外す。
- 護衛と所有者の両方で巡回する。先頭の不明個体や最初の所有者が予算を独占しない。
- 履歴全件を定期探索しない。未完操作だけを限定的に扱い、完了履歴は読み込みイベント時のUUID辞書照合を基本にする。
- 1回の観測スナップショットをGUI内で共有し、同じ護衛をカード・ヘッダー・フィルター・集計で何度もチャンク走査しない。
- 検索予算が尽きた状態を「検索したが見つからなかった」と区別する。
- Worldオブジェクトの強参照や使用済み検索カーソルを保持し続けない。

### E3. 設定の安全な再読み込み

`reloadConfig()` / `YamlConfiguration.loadConfiguration()` を呼んだだけで成功にしない。

1. 新しいconfig/messagesを一時オブジェクトに厳密に読み込む。
2. 型、有限数、整数範囲、列挙、Material/Sound、Mobリスト、相互関係を検証する。
3. 不正なら旧設定と旧メッセージをそのまま使い、問題のキーと理由を呼び出し元へ表示する。
4. すべて正常な場合だけ一括で反映する。途中までconfigだけ反映しmessagesは旧のまま、という状態を避ける。
5. タスク再起動は成功後だけ。旧タスクを止めて新タスクを1本だけ開始する。チケット上限変更も適用する。

キー不足は既定値補完で互換を保つ。「型が誤っている」と「古い設定なのでキーがない」を区別する。ユーザーの既存ファイルを丸ごと上書きしない。

**Eの完成条件:** 例外が他個体へ波及せず、毎tickログを埋めず、上限・公平性を守り、不正なreloadで正常設定を失わない。

## 9. 実装の進め方と静的確認

実装順は **A → B → C → D → E → 全経路レビュー・文書更新**。既存途中コードは活用するが、ここで指定した安全性に合わない場合は設計を修正する。未完成コードにさらにboolean分岐を足すだけで仕上げない。

各フェーズで次を記録する:

- 変更ファイルと責務。
- 状態遷移・保存順序・失敗時の動き。
- 対象の全入口（GUI、コマンド、定期処理、読込イベント、起動、終了）。
- 静的確認したこと、未実施の確認、残る制約。

コード整理時は不要import・使われなくなったヘルパーを削除し、旧READMEと矛盾するコメントを更新する。外部APIが必要なら既存のSpigot依存jar・公式API資料で確認し、推測のメソッドを実装しない。

### ビルドを行う場合

ユーザーは禁止しているのはテスト。コンパイル確認をする場合はコマンドプロンプトで、テストを走らせない:

```bat
call "<確認したMavenのbin\mvn.cmd>" -Dmaven.test.skip=true package
```

`build.bat` は現状 `clean package` を呼び、pauseもあるので、そのまま自動実行しない。MavenはPATHに見つからなかったが、`C:\Program Files\JetBrains\IntelliJ IDEA Community Edition 2023.2.5` が存在することは確認した。内部のmvn.cmdや実際のJavaバージョンはSolが読み取り確認する。環境不足をテスト成功やビルド成功と報告しない。

本番サーバーへの配置・起動はしない。成果物を作成できた場合は出力先とテスト未実施を伝える。

## 10. ユーザー向け手動確認手順の原稿

以下を最終的な実装とコマンドに合わせてREADMEまたは `MANUAL_VERIFICATION.md` に整える。**エージェントは実行しない。** 上級項目は初心者向けの基本項目と分ける。

### 準備

1. サーバーを停止して、サーバーフォルダー全体をコピーする。
2. コピーした検証用サーバーだけで新Jarを使う。本番データでファイル破損や削除の確認をしない。
3. OP権限のプレイヤーと、通常権限のプレイヤーを用意する。可能な範囲で別ワールドも使う。
4. どの操作でどうなったか、ゲーム画面とサーバーログを記録する。失敗時は対象名・時刻・コマンドを控える。

### 基本操作

| ID | ユーザーがする操作 | 期待する結果 |
|---|---|---|
| T01 | 護衛を2体迎え、一覧を開く | 名前・数・HP・状態が合う |
| T02 | 一覧を開いたまま1体を死亡させる | 数は減る。元の位置に死亡済みカード。他の護衛が勝手に詰まらない |
| T03 | 死亡済みカードを押す | 他の護衛を操作しない。整理すると通常一覧から消える |
| T04 | 遠方へ移動してから戻る | 正常な護衛が誤削除されない。上限制御で未読込なら理由が分かる |
| T05 | 詳細から契約解除する | 通常数が減り、通常Mobに戻り、履歴に残る |
| T06 | 遠方の護衛を解除し、再起動してから近づく | 解除待ちが実行され、護衛として復活しない |
| T07 | 遠方の護衛を完全削除し、再起動してから近づく | 削除待ちが実行され、一覧へ再登録されない |
| T08 | 所在不明フィルターと履歴フィルターを開く | 内容・件数・空表示が一致する。履歴カードは操作できない |
| T09 | 所在不明の護衛を解除する | 保存できた場合だけ枠が空く。再発見しても旧契約が復活しない |
| T10 | 解除した通常Mobをもう一度勧誘する | 正規の新契約として登録され、古い解除指示が再適用されない |
| T11 | 召喚・解除確定・回復を素早く複数回押す | 同じ確定が二重処理されず、上限をすり抜けない |
| T12 | 確認画面を開いた後に別の護衛を迎えて確定する | 後から追加した護衛が一括解除・削除に巻き込まれない |
| T13 | 他人の護衛を操作する | 拒否され、所有者や状態が変わらない |
| T14 | 名前入力中に対象を解除・死亡させる | 名前変更しない。アイテム・経験値の持ち出しや消費がない |
| T15 | お気に入り・相棒・仲間を設定して再起動 | 設定が維持され、終了した相棒は安全に解除される |

### 移動と保存（検証用サーバーのみ）

| ID | ユーザーがする操作 | 期待する結果 |
|---|---|---|
| T16 | 狭い部屋、低い天井、崖際、溶岩・焚火・サボテンの近くで呼び戻す | 危険な場所へ移動しない。安全候補なしの理由が出る |
| T17 | 大きさの異なる護衛を呼ぶ | 体格が収まる場所へ移動する。壁や天井で窒息しない |
| T18 | 多数の護衛を集合・連続召喚する | 同じ位置に極端に重ならず、安全候補不足は中止になる |
| T19 | 別ワールド移動禁止設定で呼び戻す | 全入口で設定が守られる |
| T20 | 設定ファイルの数値や型を誤った値にして `/bg reload` | 旧設定が維持され、誤ったキーが案内される |
| T21 | 停止後にコピー先のguards.ymlだけを壊し、正常バックアップを残して起動 | 元を保全し復旧する。削除済みが復活しない |
| T22 | 本体とバックアップの両方を復旧不能にする | 空データで上書きせず変更停止。理由がログに出る |
| T23 | 一部の護衛UUID・相棒UUIDだけを不正にする | 不正項目が保全・隔離され、正常項目が巻き添えで消えない |
| T24 | 一時的にワールドをロードしない構成で起動し、その後戻す | UUID・最終位置・警備地点が失われず、同名別ワールドに誤接続しない |
| T25 | 保存先への書込みを一時的に禁止して操作し、その後戻す | 成功と誤表示せず、破壊的操作は未実行。復旧後の再試行が反映される |
| T26 | 自動保存を0にしてから保存失敗→復旧を確認する | 通常自動保存とは独立して未保存の再試行が働く |
| T27 | チャンク上限を小さくし、複数所有者で護衛を分散する | 上限を守り、後ろの護衛・所有者も順番に確認される |
| T28 | `/bg status` と管理者用診断を見る | 数・保存状態・保留理由・チケット数が実態に合う |
| T29 | GUIでShift・数字キー・ダブルクリック・ドラッグ等を使う | GUIアイテムが持ち出せず、誤操作・増殖がない |
| T30 | 使用後ログアウト・サーバー停止・再起動を行う | 不要なチケット・タスク・GUIセッションが残らない |

操作台帳の途中切断、他プラグインからのremove・teleportキャンセル、世界読み込み順の再現は上級確認として別記する。ユーザーが環境を持っていなければ無理に本番で再現させず、「未確認」と記録する。

## 11. 完成監査チェックリスト

完了報告前に、各項目について該当ファイル・メソッドを記録し、仕様との一致を静的に確認する。テストを行っていないことは別欄に明記する。

- [ ] 状態遷移が一か所に定義され、矛盾フラグがない。
- [ ] 不明・未ロード・ワールド不在・検索予算待ちを区別する。
- [ ] 所在不明の枠確保と、解除による枠解放が一貫する。
- [ ] 通常一覧、チャット一覧、司令メニュー、上限、履歴で対象が一致する。
- [ ] 完了履歴を定期探索せず、再発見時に再登録を防ぐ。
- [ ] 削除・解除の受理前に必ず永続化し、二重適用を安全に扱う。
- [ ] バックアップ復旧で最新削除意思を失わない。
- [ ] 問い合わせ・GUI描画が変更処理を起こさない。
- [ ] 元データ・不正項目・正常バックアップを保全する。
- [ ] World未解決でも座標とPDCの警備地点が失われない。
- [ ] 保存失敗は結果・画面・管理者診断に反映される。
- [ ] 相棒・仲間・ガイドの保存にも同じ保護と再試行がある。
- [ ] 安全位置なしのフォールバックが全経路からなくなっている。
- [ ] 体格・足場・危険物・境界・重なりを扱う。
- [ ] 召喚・勧誘失敗の後始末が区別される。
- [ ] 一括操作・イベント・定期処理が個体単位で例外分離される。
- [ ] GUIセッション、確定トークン、権限、所有権を実行直前に検証する。
- [ ] 一括結果の各カテゴリ合計が対象数に一致する。
- [ ] 設定不正時は旧設定が維持され、タスクが増殖しない。
- [ ] チャンク上限・探索予算・所有者間の公平性・終了時解放を守る。
- [ ] 診断・ログ抑制・回復後の状態解除がある。
- [ ] config/messages/help/タブ補完/READMEが実装と一致する。
- [ ] `SAFETY_IMPLEMENTATION.md` を根拠に従って更新した。
- [ ] コンパイル確認の実施有無と結果を正確に記録した。
- [ ] 自動テスト・実機テストを実行していないことを明記し、手動確認手順を渡した。

最終報告は「何を改善したか」「成果物」「未実施の確認」「ユーザーが最初に確認する操作」を簡潔に伝える。「すべてのエラーが絶対に起こらない」「完全に検証済み」とは書かない。残る未実装を文書化しただけで実装完了にしない。

## 12. Solに渡す開始指示

> `SOL_IMPLEMENTATION_PLAN.md` を全体仕様として読み、現在のソースを確認して、実装途中の安全性・使いやすさ改善を最後まで完成させてください。最初に未完成箇所と現状の差分を確認し、フェーズAから順に進めてください。途中の追加コードには既知の問題があるため、そのまま完成品として扱わないでください。テストは私が行うので実行しないでください。実装後は初心者向けの手動確認手順を用意してください。通常の護衛の誤削除防止、削除・解除済み護衛の復活防止、保存破損時のデータ保護を最優先に、計画書の全項目を実装してください。
