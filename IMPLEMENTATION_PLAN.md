# BodyGuard 改良実装計画書

最終更新: 2026-09-15

## 目的

既存の護衛機能とGUI操作を維持しながら、アイテム悪用、保存時の負荷と破損、同期チャンク読み込みによるラグの危険を先に減らす。その後、巨大クラスを段階的に分割し、引き継ぎや機能追加をしやすくする。

## 作業上の制約

- 対象環境は Windows 10、Java 21、Spigot 1.21.8。
- ビルドとゲーム内テストはユーザーが実施する。この作業では実行しない。
- 既存の `config.yml`、`messages.yml`、`guards.yml` は自動で丸ごと上書きしない。
- Paper API、NMS、追加ライブラリは使用しない。
- Bukkit APIでEntityを操作する処理はメインスレッドに残す。

## 実装段階

### Phase 1: 安全性と保存処理（実装済み・ユーザー確認待ち）

1. メニュー専用アイテムの保護
   - `/bg item` に `bodyguard.item` 権限を適用する。
   - 同じプレイヤーのインベントリに専用アイテムがある場合は再配布しない。
   - 専用アイテムをクラフト材料、金床・鍛冶台の材料、コンテナへの移動、ドロップに使えないようにする。
   - 新規設定の初期素材を価値の低い `COMPASS` にする。既存設定値は勝手に変更しない。

2. `guards.yml` の保存改善
   - レジストリに変更があったときだけ保存対象にする dirty flag を導入する。
   - 自動保存間隔を設定化し、既定値を60秒にする。
   - 一括解除時の「1体ごとの保存」をやめ、最後に1回だけ保存する。
   - 一時ファイルへの保存後に置換し、直前ファイルを `guards.yml.bak` として保持する。
   - プラグイン終了時は未保存フラグに関係なく最終保存する。

3. 同期チャンク読み込みの回避
   - コマンドの全解除でも未読み込みチャンクを強制的に読み込まない。
   - 今回は「現在読み込まれている護衛だけ解除」を共通仕様とする。
   - 未読み込み護衛の解除予約は Phase 2 で追加済み。

### Phase 2: 未読み込み護衛の操作（実装済み・ユーザー確認待ち）

- `guards.yml` に解除予約状態を保存する。
- 対象チャンクが自然に読み込まれたときにPDCを除去して通常Mobへ戻す。
- 結果表示を「解除済み」「解除予約」「失敗」に分ける。
- ワールド名に加えてワールドUUIDを保存し、ワールド名変更への耐性を上げる。

実装上の補足:

- 解除予約は `guards.yml` の `release-pending` に保存する。
- 予約中のEntityはチャンク読込または通常のEntity参照時に、元の設定を復元してBodyGuard用PDCを除去する。
- 解除処理の戻り値は `ReleaseResult` にまとめ、解除済み・予約・失敗を呼び出し側で区別する。
- 保存形式はversion 2。既存のversion 1には予約値がないため、安全な既定値 `false` として読み込む。

### Phase 3: クラス分割（進行中）

- `BodyGuardGui` を一覧、詳細、名前入力、管理・確認に分ける。
- `GuardManager` をレジストリ、永続化調整、移動、戦闘、Entityメタデータに分ける。
- コマンドの権限判定とGUIの権限判定を共通サービスへ寄せる。
- 外部仕様を変えず、小さい単位で移行する。

現在までの分割:

- `BodyGuardReleaseMenu`: 解除確認画面のタイトル、注意、ボタン、背景を生成する。
- `GuardListQuery`: 一覧の絞り込み、安定した並べ替え、負傷・状態不明の集計を担当する。
- `BodyGuardGui`: 画面遷移、クリック処理、自動更新を引き続き担当する。

次の分割候補:

1. 詳細画面の描画を `BodyGuardDetailMenu` へ移す。
2. 召喚候補画面の描画を `BodyGuardSummonMenu` へ移す。
3. 一覧画面の描画を `BodyGuardListMenu` へ移す。
4. 最後にクリック処理を各画面コントローラーへ移す。

### Phase 4: 設定・公開準備（未着手）

- 設定をリロード時に検証済みオブジェクトへ読み込む。
- 未使用の `general.language` を実装するか削除する。
- Mavenのバージョンを `plugin.yml` と起動ログへ反映し、重複をなくす。
- 仮パッケージ名 `plugin.test.com.bodyGuard` の移行先を決めて変更する。

## Phase 1 の完了条件

- `/bg item` の連打で専用アイテムが増えない。
- 専用アイテムをクラフト材料や通常コンテナへ移せない。
- データ変更がない間は `guards.yml` を定期的に書き直さない。
- 一括解除で保存処理が1回だけ行われる。
- 保存失敗時に既存の `guards.yml` を直接途中まで上書きしない。
- 全解除が未読み込みチャンクを同期ロードしない。
- READMEに変更内容と手動確認方法が記載されている。

## ユーザーが行う手動確認

1. コマンドプロンプトで `build.bat` を実行し、`target\BodyGuard.jar` が作成されることを確認する。
2. 検証用サーバーへJarを入れ、起動時に例外がないことを確認する。
3. `/bg item` を2回実行し、専用アイテムが1個のままであることを確認する。
4. 専用アイテムをドロップ、作業台、金床、鍛冶台、チェストで使用できないことを確認する。
5. 護衛の召喚、名前変更、モード変更後に再起動し、状態が残ることを確認する。
6. 複数の護衛を解除し、`guards.yml` と `guards.yml.bak` が読めるYAMLとして残ることを確認する。
7. 遠方のチャンクに護衛を置いてアンロードし、全解除してもそのチャンクが読み込まれないことを確認する。
8. 解除結果に「解除済み・解除予約・失敗」の件数が表示されることを確認する。
9. 解除予約後にサーバーを再起動し、対象チャンクへ近づいた時点で通常Mobへ戻ることを確認する。
10. `guards.yml` の位置情報に `world` と `world-uuid` の両方が保存されることを確認する。

## 引き継ぎメモ

- 実装済み・未実装の状態は、この文書の各Phase見出しとGit差分を確認する。
- Phase 1とPhase 2のソース変更は完了しているが、方針によりビルドとゲーム内テストは未実施。
- Phase 3は進行中。解除確認画面と一覧検索処理は分割済みで、次は詳細画面の描画を分割する。
- クラス分割中は同じ変更でパッケージ名を移動しない。
- 非同期保存へ進める場合は、メインスレッド上でBukkitオブジェクトから保存用の値へ変換し、ワーカースレッドでは文字列または純粋なJavaデータだけを扱う。
- GUIとManagerの分割はPhase 1の挙動確認後に行い、同時にパッケージ移動をしない。
