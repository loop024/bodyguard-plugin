# BodyGuard 実装・引き継ぎ計画書

最終更新: 2026-09-15

## 目的

敵対Mobを護衛として連れ歩く既存機能を維持しつつ、安全性、保存処理、GUIの操作性を段階的に改善する。本書を作業の基準とし、途中から担当する人が「実装済み」「確認待ち」「次に触る場所」を判断できる状態にする。

## 対応環境と制約

- Windows 10、Spigot 1.21.8、Java 21。
- ビルド、テスト、ゲーム内確認はユーザーが実施する。
- Paper API、NMS、追加ライブラリは使用しない。
- 既存の `config.yml`、`messages.yml`、`guards.yml` を丸ごと上書きしない。
- BukkitのEntity操作はメインスレッドに残す。
- 既存の所有者判定、権限判定、UUIDによる対象識別を弱めない。

## 現在の状態

### Phase 1: 安全性と保存処理（実装済み・ユーザー確認待ち）

- メニュー専用アイテムの複製、投棄、コンテナ移動、材料利用を防止。
- dirty flag、自動保存間隔、一時ファイルとバックアップを導入。
- 一括解除時の保存回数を削減。
- 全解除で未読み込みチャンクを同期ロードしない。

### Phase 2: 未読み込み護衛の操作（実装済み・ユーザー確認待ち）

- 未読み込み護衛の解除予約を `guards.yml` に保存。
- チャンクが自然に読み込まれた時点で通常Mobへ戻す。
- 解除済み、解除予約、失敗を分けて表示。
- ワールド名とワールドUUIDを保存。

### Phase 3: クラス分割（進行中）

- `BodyGuardReleaseMenu`: 解除確認画面を担当。
- `GuardListQuery`: 一覧の絞り込み、並べ替え、集計を担当。
- `BodyGuardGui`: 画面遷移、描画、クリック処理、自動更新を担当したまま。

次の分割候補は、詳細画面、召喚画面、一覧画面、各クリック処理の順。表側の仕様が固まる前に大規模な移動は行わない。

### Phase 4: 設定・公開準備（未着手）

- 設定をリロード時に検証済みオブジェクトへ変換。
- 未使用の言語設定を実装または削除。
- Maven、`plugin.yml`、起動ログのバージョンを一本化。
- 仮パッケージ名 `plugin.test.com.bodyGuard` の移行先を決定。

## Phase 5: 表側グレードアップ（着手）

説明を読み込まなくても状態と操作方法を判断でき、護衛を連れ歩く楽しさを感じられる画面を目標とする。

### 5-1. 護衛カードと一覧クイック操作（実装済み・ユーザー確認待ち）

- 護衛カードを「種類、HP、行動、距離または状態」の順に統一する。
- HPは数値と10段階バーを近接表示し、低HPは色と文章の両方で警告する。
- 左クリックは従来どおり詳細画面を開く。
- 右クリックは `追従 → 待機 → 警備` の順に次のモードへ変更する。
- Shift＋左クリックは対象1体だけを呼び戻す。
- Shift＋右クリックは対象1体だけを回復する。
- 解除と名前変更は誤操作防止のため詳細画面だけに残す。
- 操作後もページ、絞り込み、並べ替えを維持する。
- 実行時に所有者、権限、Entityの読み込み状態を再確認する。

完了条件:

- カードだけで種類、HP、モード、距離または利用不能理由が分かる。
- 表示されたクリック案内と動作が一致する。
- 権限不足、未読み込み、全回復済みで別の護衛やアイテムへ影響しない。
- クイック操作結果が一覧の結果欄に表示される。
- 詳細画面の既存操作も残る。

### 5-2. 画面デザインと視覚言語の統一（未着手）

目的:

- チェストGUIの制約内で、どこが情報、通常操作、移動、危険操作なのかを色と配置から判断できるようにする。
- 色だけに依存せず、アイコンと文章でも状態を区別できるようにする。

画面別テーマ:

- 護衛一覧: 濃い青と水色。日常的な管理画面として落ち着いた配色にする。
- 召喚: 紫とマゼンタ。新しい護衛を迎える特別な操作として区別する。
- 護衛詳細: 基本は青。健康状態に応じて概要アイコンや周辺色を緑、黄、赤へ変える。
- 管理: 黄色と橙。設定や一括操作の画面であることを示す。
- 解除確認: 暗い赤。ほかの画面と明確に分離し、危険操作だと分かるようにする。
- 成功結果: 緑、注意結果: 黄、失敗結果: 赤、利用不能: 灰色に統一する。

配置規則:

- 上段は画面説明、中央は対象と主要操作、下段は戻る、更新、ページ移動、閉じるに固定する。
- 戻るは左下、閉じるは右下、更新は右下寄り、危険操作は通常操作から1スロット以上離す。
- 背景ガラスを一色で埋めず、外枠、見出し、操作領域で濃淡を変える。
- 選択中のモードは元のリード、金床、盾を維持し、緑の名前、選択中文字、控えめな光で示す。
- 権限不足や利用不能は灰色アイコンへ置き換え、理由と次にできることをLoreへ表示する。

表示色の役割:

- 緑: 健康、成功、選択中。
- 黄: 軽傷、注意、確認が必要。
- 赤: 瀕死、失敗、解除などの危険操作。
- 水色: 詳細、移動、呼び戻し、ページ移動。
- 紫: 名前、召喚、個性に関する操作。
- 灰色: 未読み込み、権限不足、現在利用不能。

完了条件:

- どの画面でも、戻る、閉じる、更新、危険操作が同じ考え方で配置されている。
- 成功、注意、失敗、利用不能を色、素材、文章のうち最低2種類で識別できる。
- 既存 `messages.yml` に新規キーがなくてもコード内フォールバックで表示できる。

### 5-3. お気に入り護衛と相棒（未着手）

目的:

- 護衛が増えても重要な1体をすぐ見つけられ、単なるMob一覧ではなく愛着のある相棒として扱えるようにする。

仕様:

- 各護衛にお気に入り状態を保存できるようにする。お気に入りは複数体設定可能とする。
- お気に入りとは別に、所有者ごとに1体だけ「相棒」を設定できるようにする。
- 詳細画面に「お気に入り登録／解除」と「相棒に設定／解除」を配置する。
- 一覧の並べ替え「標準」では、お気に入りを先頭へ置き、その中では元の安定した順番を維持する。
- 一覧上部に `すべて → お気に入り → 負傷中 → 追従中 → 待機中 → 警備中 → 状態不明` のフィルターを用意する。
- お気に入り護衛は名前に星を付け、アイコンを控えめに光らせる。
- 相棒は星と「相棒」表示でお気に入りより強く区別するが、常時パーティクルは出さない。
- 相棒が死亡または解除された場合は相棒設定を解除し、所有者へ1度だけ知らせる。
- 相棒が未読み込みの場合も設定自体は保持し、勝手に別の護衛へ変更しない。
- 中クリックをお気に入り切替に使う案は、Minecraftクライアントやゲームモードによる操作差を確認してから採用する。採用しない場合は詳細画面だけで変更する。

保存:

- `GuardData`、EntityのPDC、`guards.yml` にお気に入り状態を保存する。
- 所有者ごとの相棒UUIDは保存責任を明確にし、削除時に孤立値を残さない。
- 旧保存データではお気に入り `false`、相棒なしを既定値にする。
- 保存形式のversionを更新する場合は、旧versionからの読み込み方法を文書化する。

完了条件:

- 再起動後もお気に入りと相棒が維持される。
- お気に入りフィルター、標準並べ替え、表示上の星が同じ保存値を参照する。
- 他人の護衛をお気に入りまたは相棒にできない。
- 相棒の死亡、解除、データ不整合時に別護衛へ誤適用されない。

### 5-4. 護衛の頭上表示（未着手）

目的:

- GUIを開かなくても、目の前の護衛の名前、HP、行動モードを把握できるようにする。

表示案:

```text
★ ゾンビ護衛 01
❤ 16/20｜追従
```

仕様:

- 表示レベルを `名前だけ`、`名前＋HP`、`名前＋HP＋モード`、`非表示` から設定できるようにする。
- 相棒は名前先頭に星を付ける。通常のお気に入りとの記号差は一覧と統一する。
- 健康は緑、軽傷は黄、瀕死は赤、HP不明は灰色とし、数値または「不明」も必ず表示する。
- 追従、待機、警備は日本語名で表示する。
- 長い名前は表示崩れを避けるため安全に省略し、詳細画面では全文を確認できるようにする。
- ArmorStandなどの追加Entityを使用する場合は、護衛との紐付け、チャンクアンロード、死亡、解除、プラグイン停止時の確実な削除を設計する。
- 標準のカスタム名だけで複数行を実現できない場合は、互換性と残留Entityの危険を比較し、一行表示へ縮小する選択肢も残す。
- 他プラグインが名前表示を変更する可能性を考慮し、設定で完全に無効化できるようにする。

設定候補:

- `display.nameplate.enabled`
- `display.nameplate.mode: NAME | NAME_HEALTH | NAME_HEALTH_MODE | HIDDEN`
- `display.nameplate.range`
- `display.nameplate.low-health-ratio`

完了条件:

- 召喚、読込、アンロード、死亡、解除、再起動で表示が重複または残留しない。
- 表示内容が実際のHPとモードに追従する。
- 無効設定では追加表示処理が動作しない。

### 5-5. 状態変化のパーティクルとサウンド（未着手）

目的:

- 操作が成功したことを、チャットを読まなくても短い演出で感じられるようにする。

演出案:

- 召喚: 青または紫の粒子がMobの周囲へ集まる。
- 追従へ変更: 足元に短い緑の輪。
- 待機へ変更: 足元に短い黄色の輪。
- 警備へ変更: 青い盾を連想する円または壁状の演出。
- 回復: ハート粒子と柔らかい成功音。
- 呼び戻し: ポータル系の粒子と移動音。
- 解除: 赤または灰色の粒子が外側へ離れる。敵対する可能性を派手な成功演出で隠さない。
- 瀕死: 小さな赤い警告。常時ではなく十分な間隔を設ける。

制御:

- 毎tick生成せず、操作成功時または状態が閾値をまたいだ時だけ再生する。
- 失敗時は対象Mob側に演出を出さず、操作した本人へ現在の失敗音と結果表示だけを返す。
- ワールド演出とGUI個人音を別設定にし、管理者が個別に無効化できるようにする。
- パーティクル数、表示範囲、瀕死警告間隔を上限付き設定として検証する。
- 自動更新では音や成功演出を再生しない。

設定候補:

- `effects.guard-feedback.enabled`
- `effects.guard-feedback.summon`
- `effects.guard-feedback.mode-change`
- `effects.guard-feedback.heal`
- `effects.guard-feedback.recall`
- `effects.guard-feedback.release`
- `effects.guard-feedback.low-health.enabled`
- `effects.guard-feedback.low-health-cooldown-seconds`

完了条件:

- 1回の操作で演出が二重再生されない。
- 自動更新や画面再描画だけでは演出されない。
- 設定無効時に粒子と音が再生されない。
- 多数の護衛がいても常時大量の粒子を生成しない。

### 5-6. 護衛プロフィール、戦績、称号（未着手）

目的:

- 戦闘能力を不用意に変更せず、長く連れ歩いた記録を見せて育成感を作る。

プロフィール表示:

- 契約日時。
- 一緒にいた累計時間。
- 倒した敵の数。
- 所有者を守るために戦闘へ入った回数。
- 受けた累計ダメージ。
- 呼び戻した回数。
- 回復された回数。
- 現在のお気に入り、相棒状態。
- 現在の称号と、次の称号までの進行状況。

称号候補:

- `新米護衛`: 契約直後の初期称号。
- `頼れる相棒`: 一定時間一緒に行動した護衛。
- `不屈の番人`: 大きな累計ダメージを受けながら生存している護衛。
- `百戦錬磨`: 敵を一定数倒した護衛。
- `帰還の達人`: 呼び戻された回数が一定値に達した護衛。

記録ルール:

- Bukkitイベントから正確に判定できる値だけを記録し、推測値を保存しない。
- 「助けた回数」は判定が曖昧なため、所有者防衛を開始した回数など、実装可能な定義へ明文化してから採用する。
- 累計時間はサーバー停止時間を含めず、所有者と護衛が読み込み済みで活動可能な時間だけを加算する。
- 同一イベントの重複カウントを防ぐ。
- 称号は最初は表示だけとし、攻撃力、速度、HPなどの能力補正を付けない。
- 称号条件は設定化する場合も、負数や極端な値を検証する。

画面:

- 詳細画面からプロフィール画面へ移動するボタンを追加する。
- 27または45スロットで、基本情報、戦闘記録、行動記録、称号を領域分けする。
- 記録がまだない項目も `0` または「記録なし」と明示し、項目自体を不規則に消さない。

保存:

- 各統計値と契約日時を `GuardData`、PDC、`guards.yml` のどこへ保存するか項目ごとに統一する。
- 数値は0以上に補正し、破損値や旧データでは安全な既定値を使う。
- 保存versionと移行方法を更新する。

完了条件:

- 再起動後も戦績が維持される。
- 同じ戦闘や操作が二重計上されない。
- 称号条件とプロフィール表示が同じ値を参照する。
- 称号によって既存の戦闘能力が変化しない。

### 5-7. 護衛状態通知とプレイヤー別通知設定（未着手）

目的:

- GUIを開いていない時も重要な変化だけを知らせ、頻繁な通知でプレイを妨げないようにする。

通知対象:

- `瀕死`: HPが設定した割合以下へ初めて下がった時。
- `敵を発見`: 警備中の護衛が新しい戦闘対象を得た時。
- `遠く離れた`: 同じワールドで設定距離を超えた時。
- `見失った`: 読み込み済みから未読み込みになっただけでは即通知せず、一定条件を満たした時。
- `死亡`: 護衛が死亡した時。
- `解除予約完了`: 未読み込み護衛が実際に通常Mobへ戻った時。
- `相棒解除`: 相棒が死亡または解除され、相棒なしになった時。

表示例:

- `ゾンビ護衛 01 が瀕死です（HP 4/20）`
- `警備中のゾンビ護衛 01 が敵を発見しました`
- `ゾンビ護衛 01 が遠く離れています（128m）`
- `ゾンビ護衛 01 の状態を確認できません`
- `ゾンビ護衛 01 が死亡しました`

通知方式:

- チャット、アクションバー、タイトルの候補から、通知種類に適した方式を割り当てる。
- 死亡など重大な通知はチャットへ残し、短期状態はアクションバーを優先する。
- タイトルは視界を大きく遮るため、既定では死亡や相棒喪失など重大イベントだけに限定するか無効にする。
- 既存アクションバーの照準表示と競合しないよう、通知の優先順位と表示時間を管理する。

プレイヤー別設定:

- `すべて`: 利用可能な全通知を受け取る。
- `重要のみ`: 瀕死、死亡、相棒喪失、解除予約完了などに限定する。既定値候補。
- `なし`: 状態通知を停止する。ただしコマンドやGUIを操作した本人への結果は停止しない。
- GUIの設定画面から切り替え、再ログインと再起動後も保持する。
- 管理者用のサーバー既定値と、プレイヤーが上書きできる範囲を設定する。

頻度制限:

- 同じ護衛、同じ通知種類ごとにクールダウンを持つ。
- 瀕死状態が続いている間は繰り返さず、回復して閾値を上回った後に再び低下した時だけ再通知する。
- 敵発見は対象が頻繁に変わっても短時間に連投しない。
- 遠距離と見失いは状態遷移時だけ通知し、定期タスクのたびに送らない。

完了条件:

- 同じ状態が継続しているだけで通知が連投されない。
- `なし` でも操作結果と重大なサーバーエラー通知まで消えない。
- プレイヤー別設定が再ログインと再起動後も維持される。
- 照準中アクションバーと状態通知が互いを高速で上書きしない。

### 5-8. 簡易司令メニュー（未着手）

目的:

- 日常的な全体指示を少ないクリックで実行し、細かい管理だけを護衛一覧へ分離する。

画面案:

- 27スロットの司令メニューとする。
- `全員追従`: 読み込み済みで操作可能な全護衛を追従へ変更。
- `全員待機`: 読み込み済みで操作可能な全護衛を現在位置で待機。
- `全員警備`: 読み込み済みで操作可能な全護衛を現在位置中心の警備へ変更。
- `全員集合`: 既存の全員呼び戻しを実行。
- `負傷者を回復`: 既存の全員回復を実行。
- `護衛一覧`: 現在の54スロット一覧を開く。
- `新規召喚`: 召喚候補画面を開く。
- `設定`: 通知や頭上表示などのプレイヤー設定を開く。
- `閉じる`: 右下へ固定。

導線:

- メニュー専用コンパスの右クリックは司令メニューを開く候補とする。
- `/bg` と `/bg menu` を司令メニューへ変更するか、従来どおり一覧を開くかを互換性の観点から決定する。
- 互換性を優先する場合は `/bg` を一覧のまま維持し、コンパスまたは `/bg command` から司令メニューを開く。
- 最終決定を `README.md` とヘルプへ明記する。

安全性:

- 各操作アイコンに対象総数、実行可能数、未読み込み数を表示する。
- 全体モード変更はクリック直後に実行する場合でも、結果欄へ成功、対象外、失敗件数を表示する。
- 権限不足は灰色表示にし、実行時にも再確認する。
- 未読み込み護衛を全体モード変更のために強制ロードしない。
- 解除は司令メニューへ直接置かず、管理画面と確認画面を経由する。

完了条件:

- 日常的な全員集合、回復、モード変更へ3クリック以内で到達できる。
- 対象数と結果件数が表示される。
- 未読み込み護衛や権限不足が誤って成功扱いにならない。
- 従来の一覧、詳細、召喚、管理画面へ戻れる。

### 5-9. 初回チュートリアルと再表示可能なガイド（未着手）

目的:

- 初めて使うプレイヤーがコマンド一覧を読まなくても、最初の護衛と基本モードまで到達できるようにする。

初回案内:

1. `護衛を迎える`: 召喚候補を開く方法と、野生Mobを見て `/bg recruit` を使う方法を案内する。
2. `護衛を操作する`: 一覧の左、右、Shift＋左右クリックと、空手でしゃがみ＋右クリックする方法を案内する。
3. `行動を選ぶ`: 追従、待機、警備の違いと、迷った場合は追従を選ぶことを案内する。

仕様:

- 初めて `/bg` またはメニューアイテムを使ったプレイヤーにだけ表示する。
- 1画面へ情報を詰め込まず、3段階程度のページまたはガイド画面に分ける。
- `次へ`、`前へ`、`ガイドを終了`、`次回から表示しない` を用意する。
- 初回中に閉じても、明示的に「次回から表示しない」を選ぶまでは次回再表示する。
- 司令メニューまたは一覧の操作ガイドからいつでも再表示できる。
- チュートリアル中でも強制的にMobを召喚したり、コマンドを実行したりしない。
- 召喚または勧誘権限がない場合は利用可能な方法だけを案内し、進行不能にしない。
- サーバー管理者が初回表示を無効化できる設定を用意する。

保存:

- プレイヤーUUIDごとにチュートリアル完了状態を保存する。
- `次回から表示しない` と「最後まで読んだ」を区別する必要があるか、実装前に保存値を決める。
- データがない既存プレイヤーへ突然全員表示するか、新規参加者だけにするかを設定可能にする。

完了条件:

- 新規プレイヤーがガイドから召喚画面または勧誘方法へ到達できる。
- 明示的に非表示へした後は再ログイン、再起動後も自動表示されない。
- 操作ガイドからいつでも再表示できる。
- 権限不足でも実行不可能な手順だけを要求しない。

## Phase 5 推奨実装順

1. 5-1 護衛カードと一覧クイック操作。
2. 5-2 画面デザインと視覚言語の統一。
3. 5-5 状態変化のパーティクルとサウンド。
4. 5-8 簡易司令メニュー。
5. 5-9 初回チュートリアル。
6. 5-3 お気に入り護衛と相棒。
7. 5-4 護衛の頭上表示。
8. 5-7 護衛状態通知とプレイヤー別通知設定。
9. 5-6 護衛プロフィール、戦績、称号。

5-1、5-2、5-5、5-8は既存データ形式への影響が比較的小さい。5-3、5-6、5-7、5-9は新しい永続データが必要なため、保存versionと移行設計を同時に行う。5-4は表示用Entityを使う可能性があるため、残留防止の設計を先に固める。

## 当初提案との対応表

当初提示した表側の改良案を削らず、次の場所へ割り当てている。

1. 護衛カードを一目で読めるデザイン: Phase 5-1、5-2。
2. 一覧からの直接操作: Phase 5-1。
3. お気に入り護衛と相棒: Phase 5-3。
4. 護衛の頭上表示強化: Phase 5-4。
5. 状態変化のパーティクル演出: Phase 5-5。
6. 画面ごとのテーマカラー: Phase 5-2。
7. 護衛ステータス、戦績、称号: Phase 5-6。
8. 護衛の状態通知と通知頻度設定: Phase 5-7。
9. 簡易コマンドホイール風の司令GUI: Phase 5-8。
10. 初回チュートリアルと再表示ガイド: Phase 5-9。

## 実装時の共通ルール

- 新規表示文言は `messages.yml` の `gui` 配下へ追加し、コードにも日本語フォールバックを残す。
- 一覧クリック処理では `InventoryClickEvent#getClick()` を渡し、スロット番号だけで操作を決めない。
- 一覧に保持した護衛UUIDを使い、表示名や一覧番号から対象を検索しない。
- 自動更新ではUUIDとスロットの対応を変えず、再抽出は画面を開き直す時だけ行う。
- お気に入り実装時は `GuardData`、PDC、`guards.yml` の値を一致させ、旧データの既定値を `false` にする。

## ユーザーが行う確認

1. コマンドプロンプトで `build.bat` を実行し、Jarが作成されることを確認。
2. 検証サーバーへJarを入れ、起動時に例外がないことを確認。
3. `/bg` で一覧、詳細、召喚、管理、解除確認が開くことを確認。
4. 護衛カードのHP、モード、距離が実際の状態と合うことを確認。
5. 左、右、Shift＋左、Shift＋右クリックが表示どおり動くことを確認。
6. 権限を外したプレイヤーと未読み込み護衛でも安全に失敗することを確認。
7. 操作後にページ、絞り込み、並べ替えが維持されることを確認。

## 引き継ぎメモ

- Phase 1と2はコード実装済みだが、ユーザーのゲーム内確認待ち。
- Phase 3は途中。表側の変更中に同時にパッケージ移動を行わない。
- Phase 5-1はコードと表示文言を実装済み。次の候補は5-2の画面配色統一。
- ビルドやテストを実行したものとして扱わない。確認結果はユーザーから受け取って本書へ反映する。
