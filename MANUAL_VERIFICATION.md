# BodyGuard 手動確認手順

この文書は、BodyGuard の安全性改善をサーバー管理者が自分で確認するための手順です。Codex はテスト、Maven ビルド、Minecraft サーバー起動、Jar の配置を実行していません。

## 先に準備すること

本番サーバーではなく、サーバーフォルダーをコピーした検証用サーバーで行ってください。

1. Spigot 1.21.8 と Java 21 が使える検証用サーバーを用意する。
2. OP の確認用プレイヤーと、通常権限の確認用プレイヤーを用意する。
3. 新しい Jar を配置する前に、検証用サーバーを停止してフォルダーをバックアップする。
4. サーバーを起動したら、`/bg status` と `/bg help` を最初に実行する。
5. 失敗したときは、対象の護衛名、実行した操作、時刻、画面表示、サーバーログを記録する。

各項目は、確認できたら `[ ]` を `[x]` に変えてください。再現できない項目は無理に本番で試さず「未確認」と記録します。

## 基本確認

### T01 護衛を2体迎えて一覧を開く

操作: `/bg summon zombie` などで2体を迎え、`/bg` を開く。

期待: 護衛数が2体、名前が重複せず、読み込み済みならHPと状態が表示される。

失敗時: `/bg status` の契約数と一覧の数を記録する。

### T02 一覧を開いたまま1体を死亡させる

操作: 検証用の敵Mobで護衛を1体だけ死亡させる。

期待: 通常の契約数は1体に減り、死亡済みの記録は履歴に残る。他の護衛のカードが別の対象へ勝手に変わらない。

失敗時: 死亡した護衛UUID、残った護衛名、履歴の表示を記録する。

### T03 死亡済みカードを押す

操作: 管理画面から「処理待ち・履歴」を開き、死亡済みカードをクリックする。

期待: 履歴は表示専用で、他の護衛のモード変更・回復・移動・削除が起きない。

### T04 遠方へ移動してから戻る

操作: 護衛から離れ、別の場所へ移動してから `/bg` を開く。

期待: UUID検索に失敗しただけで護衛が削除されない。未読み込み、確認中、所在不明のどれかが表示され、次の操作が分かる。

### T05 詳細から契約解除する

操作: 護衛詳細の「この護衛の契約を解除」を押し、確認画面で確定する。

期待: 通常一覧の契約数が減り、Entity は通常Mobに戻り、履歴に解除済みとして残る。

### T06 遠方の護衛を解除して再起動する

操作: 遠方の護衛を解除予約にし、サーバーを通常停止して再起動する。その後、対象チャンクへ近づく。

期待: 読み込み時に解除が実行され、護衛として再登録・復活しない。

### T07 遠方の護衛を完全削除して再起動する

操作: 検証用データで遠方の護衛を完全削除予約にし、再起動して対象チャンクへ近づく。

期待: 読み込み時にMobが削除され、一覧やPDCから護衛として復活しない。

### T08 所在不明フィルターと履歴フィルターを開く

操作: 管理画面から「所在不明の護衛」と「処理待ち・履歴」を順に開く。

期待: 件数とカードの内容が一致する。履歴カードには操作説明がなく、クリックしても対象を変更しない。空の場合は空表示になる。

### T09 所在不明の護衛を解除する

操作: 所在不明フィルターの護衛を選び、解除を確定する。

期待: 解除意思の保存に成功した場合だけ解除予約として契約枠が解放される。再発見しても古い契約へ戻らない。

### T10 解除した通常Mobをもう一度勧誘する

操作: T05 で解除した通常Mobを見て `/bg recruit` を実行する。

期待: 正規の新契約として登録され、古い解除記録が新契約へ適用されない。

### T11 召喚・解除確定・回復を素早く複数回押す

操作: 召喚候補、解除確認、回復ボタンを短時間に連打する。

期待: 1回の確定が二重実行されず、護衛数の上限をすり抜けず、同じ護衛が二度削除されない。

### T12 確認画面を開いた後に別の護衛を迎える

操作: 全解除または全削除の確認画面を開き、別の護衛を追加してから確認する。

期待: 確認画面を開いた時点のUUIDだけが対象で、後から追加した護衛は巻き込まれない。

### T13 他人の護衛を操作する

操作: 通常権限のプレイヤーで他人の護衛を右クリックし、詳細・解除・回復・モード変更を試す。

期待: 拒否され、所有者、モード、名前、保存状態が変わらない。

### T14 名前入力中に対象を解除・死亡させる

操作: 金床の名前入力画面を開いたまま対象を解除または死亡させ、確定欄を押す。

期待: 名前変更されない。入力用アイテムや経験値が不正に持ち出されない・消費されない。

### T15 お気に入り・相棒・仲間を設定して再起動する

操作: 1体をお気に入りと相棒に設定し、別プレイヤーを仲間に追加して再起動する。

期待: 設定が維持される。相棒の護衛を解除・死亡させた場合は、相棒だけが安全に解除される。

## 移動・保存・負荷の確認

### T16 狭い場所や危険な場所で呼び戻す

操作: 低い天井、狭い部屋、崖際、溶岩、焚火、サボテンの近くで `/bg tp` または単体呼び戻しを行う。

期待: 危険なブロック、壁の中、境界の外へ移動しない。安全地点がない場合は移動せず、失敗表示になる。

### T17 大きさの異なる護衛を呼ぶ

操作: 対応設定を検証用に変更し、体格の異なるMobを呼び戻す。

期待: 体全体が収まる場所へ移動し、壁・天井・他のMobと重ならない。

### T18 多数の護衛を集合・連続召喚する

操作: 検証用設定で複数体を連続召喚し、全員集合を行う。

期待: 同じ地点へ極端に重ならず、安全地点が不足した護衛は無理に移動しない。Entity密集による不自然な死亡がない。

### T19 別ワールド移動禁止設定を確認する

操作: `config.yml` の `teleport.different-world: false` にして `/bg tp`、追従、GUIの単体呼び戻しを確認する。

期待: どの入口でも別ワールドへ勝手に移動しない。確認後は設定を元に戻す。

### T20 不正な設定で `/bg reload` する

操作: 検証用コピーの `config.yml` の数値を文字列にするなどして `/bg reload` を実行する。

期待: エラー表示になり、直前の正常設定、既存タスク、メッセージが維持される。設定修正後の再読み込みで正常に戻る。

### T21 壊れた guards.yml と正常なバックアップで起動する

操作: サーバー停止後、検証用コピーの `guards.yml` だけを壊し、正常な `guards.yml.bak` を残して起動する。

期待: バックアップから復旧し、壊れた元ファイルが `guards.yml.damaged-...` として保全される。解除・削除済みの記録が復活しない。

### T22 本体とバックアップを両方復旧不能にする

操作: 検証用コピーで `guards.yml` と `guards.yml.bak` の両方を壊して起動する。

期待: 空データで上書きせず、BodyGuard の起動が安全に停止し、ログに復旧不能の理由が残る。確認後は正常なバックアップへ戻す。

### T23 一部のUUIDだけを不正にする

操作: 検証用コピーで1件の護衛UUID、相棒UUID、仲間UUIDのいずれかを不正な文字列に変更して起動する。

期待: 不正項目だけが警告・隔離または無視され、正常な護衛・正常なプレイヤー設定が巻き添えで消えない。

### T24 ワールドを読めない状態から戻す

操作: 検証用コピーで対象ワールドを一時的に外して起動し、保存位置を確認した後、ワールドを戻して再起動する。

期待: ワールドUUID、最終位置、警備地点が保持される。同名の別ワールドへ自動接続されない。

### T25 保存先を書込み不可にして操作する

操作: 検証用コピーで保存先への書込みを一時的に禁止し、召喚、モード変更、解除を試す。その後、書込みを戻して再試行する。

期待: 保存失敗を成功と表示せず、破壊的操作は保留または中止になる。書込みを戻した後の再試行で反映される。

### T26 自動保存を0にして再試行する

操作: `storage.autosave-seconds: 0` にし、保存失敗を一度発生させてから保存先を戻す。

期待: 通常の自動保存が無効でも、失敗した保存が独立した再試行で回復する。

### T27 チャンク上限と所有者間の公平性を確認する

操作: `max-loaded-chunks`、`max-loaded-chunks-per-owner` を小さくし、複数プレイヤーの護衛を別々の場所へ置く。

期待: 上限を超えてチケットを持たず、同じプレイヤーだけを処理し続けない。後ろの護衛・所有者も順番に確認される。

### T28 `/bg status` を確認する

操作: プレイヤーで `/bg status`、OPまたはコンソールで `/bg status server` を実行する。

期待: 契約数、利用可能・未読み込み・所在不明、解除・削除待ち、隔離、保存結果、操作台帳、サーバー診断のチャンク数が確認できる。

### T29 GUIからアイテムを持ち出そうとする

操作: GUIでShiftクリック、数字キー、ダブルクリック、ドラッグ、オフハンド交換、上段と下段の入れ替えを試す。

期待: GUIのアイテムを持ち出せず、増殖や通常インベントリへの移動がない。

### T30 ログアウト・停止・再起動を行う

操作: GUIを開いたままログアウトし、通常停止、再起動を行う。

期待: 古いGUI、金床入力、結果表示、アクションバー、不要なチャンクチケット、重複タスクが残らない。再ログイン後に新しいGUIを開ける。

## 上級確認

次は、別プラグインによるEntity削除・テレポートキャンセル、世界の読み込み順変更、Entity読込イベントの途中切断、再起動直前の解除受付を確認する項目です。環境を用意できない場合は未確認で問題ありません。推測で「成功」と記録しないでください。

## テスト結果の記録例

| ID | 結果 | メモ |
| --- | --- | --- |
| T01 | 未確認 | 例: Jar配置後に実施 |
| T02 | 未確認 |  |
| T03 | 未確認 |  |

失敗した場合は、`plugins/BodyGuard/guards.yml`、`operations.yml`、`quarantine.yml` を上書きせず、サーバーログと検証用フォルダーを保全してから相談してください。
